codeFile: .poseidon-constants.pact
data:
publicMeta:
  chainId: "{{chain}}"
  sender: "{{sender}}"
  gasLimit: 150000
  gasPrice: 0.00000001
  ttl: 3600
networkId: "{{network}}"
signers:
  - public: "{{sender-key}}"
type: exec
